# Hướng dẫn sử dụng giao diện fe-5173

Tài liệu này mô tả các bước thao tác trên giao diện fe-5173 bằng ngôn ngữ đời thường, dành cho AI dùng để tra cứu và hướng dẫn lại người học.

Khi hướng dẫn người dùng, ưu tiên dùng đúng tên nút đang hiện trên màn hình. Ví dụ: nói "bấm `Đăng nhập`", "bấm `Tiếp tục học`", "bấm `Nộp bài`" thay vì dùng từ kỹ thuật.

## Cách hiểu chung

- "Bấm" có nghĩa là click chuột trên máy tính hoặc chạm bằng tay trên điện thoại.
- Nếu người dùng không thấy một nút nào đó, có thể tài khoản của họ chưa được cấp quyền hoặc đơn vị của họ chưa bật phần đó.
- Trên máy tính, các mục chính thường nằm ở thanh menu phía trên.
- Trên điện thoại, các mục chính thường nằm ở thanh menu phía dưới hoặc trong nút ba gạch.
- Nút `X` dùng để đóng cửa sổ đang mở.
- Nút mũi tên quay lại dùng để trở về màn hình trước.
- Nút hình kính lúp dùng để tìm kiếm.
- Nút hình chuông dùng để xem thông báo.
- Nút ảnh đại diện ở góc trên bên phải dùng để mở các lựa chọn về tài khoản.
- Nút hình mặt trăng hoặc mặt trời dùng để đổi sáng tối.
- Nút hình bảng màu dùng để đổi màu giao diện nếu phần này đang được bật.
- Nút hình mũi tên lên, nếu xuất hiện, dùng để lên đầu trang.
- Nút hình tải xuống dùng để tải tài liệu về máy.

## 1. Đăng nhập

Khi người dùng muốn vào hệ thống:

1. Mở trang fe-5173.
2. Ở màn hình `Đăng nhập vào E-learning`, bấm vào ô `Địa chỉ email`.
3. Nhập email đã được cấp.
4. Bấm vào ô `Mật khẩu`.
5. Nhập mật khẩu.
6. Nếu muốn kiểm tra mật khẩu vừa nhập, bấm nút hình con mắt ở cạnh ô mật khẩu.
7. Bấm `Đăng nhập`.
8. Nếu đăng nhập thành công, hệ thống sẽ đưa người dùng vào trang học tập.

Khi người dùng quên mật khẩu:

1. Ở màn hình đăng nhập, bấm `Quên mật khẩu?`.
2. Đọc hướng dẫn hiện ra trên màn hình.
3. Nếu muốn quay lại màn hình đăng nhập, bấm `Quay lại đăng nhập`.

Khi màn hình có nút đăng nhập bằng tài khoản ngoài:

1. Ở màn hình đăng nhập, tìm nút có dạng `Đăng nhập bằng ...`.
2. Bấm nút đó.
3. Làm theo hướng dẫn đăng nhập của trang được mở ra.

## 2. Đăng ký tài khoản mới

Khi người dùng muốn tạo tài khoản:

1. Ở màn hình đăng nhập, bấm `Đăng ký ngay`.
2. Ở màn hình đăng ký, nhập `Họ`.
3. Nhập `Tên`.
4. Nhập `Địa chỉ email`.
5. Nhập `Mật khẩu`.
6. Nhập lại mật khẩu ở ô `Xác nhận mật khẩu`.
7. Bấm nút hình con mắt nếu muốn xem lại mật khẩu vừa nhập.
8. Tích vào ô đồng ý với điều khoản nếu có hiển thị.
9. Bấm `Đăng ký`.
10. Nếu thấy màn hình `Đăng ký thành công!`, chờ quản trị viên duyệt tài khoản.
11. Bấm `Quay về đăng nhập` để trở lại màn hình đăng nhập.

Khi muốn bỏ đăng ký và quay lại:

1. Bấm `Quay lại`.
2. Hệ thống đưa người dùng về màn hình đăng nhập.

## 3. Bắt đầu sau khi đăng nhập

Khi người dùng đăng nhập lần đầu hoặc thấy cửa sổ chào mừng:

1. Đọc lời chào trên màn hình.
2. Bấm `Bắt đầu học ngay`.
3. Sau đó người dùng có thể chọn khóa học hoặc xem phần học tiếp.

Các mục chính thường dùng:

- Bấm `Khám phá` để về trang tổng quan học tập.
- Bấm `Chương trình học` để xem danh sách khóa học.
- Bấm `Thư viện` để tìm tài liệu.
- Bấm `Danh hiệu` để xem huy hiệu và tiến độ, nếu mục này có hiển thị.
- Trên điện thoại, bấm `Khám phá`, `Khóa học`, `Thư viện`, hoặc `Danh hiệu` ở thanh dưới cùng.

## 4. Dùng trang Khám phá

Trang `Khám phá` là nơi người dùng xem nhanh các nội dung học tập đang cần chú ý.

Khi người dùng muốn học tiếp khóa đang học:

1. Bấm `Khám phá`.
2. Tìm khu vực `Tiếp tục học`.
3. Nếu có nhiều khóa học, bấm vào ô `Tìm kiếm khóa học...`.
4. Nhập tên khóa học cần tìm.
5. Trên thẻ khóa học, bấm `Tiếp tục học`.
6. Hệ thống mở bài học đang học dở hoặc khóa học tương ứng.

Khi người dùng chưa có khóa học nào ở phần học tiếp:

1. Bấm `Khám phá`.
2. Tìm phần `Tiếp tục học`.
3. Nếu thấy thông báo chưa có khóa học, bấm nút dẫn sang phần tìm khóa học nếu nút đó xuất hiện.
4. Hoặc bấm `Chương trình học` trên thanh menu để tự chọn khóa học.

Khi người dùng muốn xem khóa học được đề xuất:

1. Bấm `Khám phá`.
2. Tìm khu vực `Đề xuất dành cho bạn`.
3. Bấm `Bắt đầu ngay` để đi tới danh sách khóa học.
4. Hoặc bấm `Xem tất cả` để xem thêm nội dung được đề xuất.

Khi người dùng muốn xem danh hiệu nhanh từ trang Khám phá:

1. Bấm `Khám phá`.
2. Tìm thẻ `Danh hiệu`.
3. Bấm vào danh hiệu đã đạt để xem hình lớn hơn.
4. Bấm `X` để đóng.
5. Bấm `Xem tất cả danh hiệu` để mở trang danh hiệu đầy đủ.

## 5. Tìm và bắt đầu khóa học

Khi người dùng muốn tìm khóa học:

1. Bấm `Chương trình học` trên thanh menu.
2. Bấm vào ô `Tìm khoá học...`.
3. Nhập tên khóa học hoặc từ khóa muốn tìm.
4. Xem các khóa học hiện ra bên dưới.

Khi người dùng muốn lọc khóa học theo trạng thái:

1. Bấm `Chương trình học`.
2. Chọn một trong các nút `Tất cả`, `Đang học`, `Đã hoàn thành`, hoặc `Chưa học`.
3. Danh sách khóa học sẽ đổi theo lựa chọn.

Khi người dùng muốn lọc theo danh mục:

1. Bấm `Chương trình học`.
2. Bấm `Danh mục`.
3. Chọn danh mục muốn xem.
4. Nếu muốn bỏ lọc, chọn `Tất cả danh mục` hoặc quay lại `Tất cả`.

Khi người dùng thấy một nhóm khóa học và muốn xem toàn bộ nhóm đó:

1. Bấm `Chương trình học`.
2. Tìm nhóm khóa học cần xem.
3. Bấm `Xem tất cả` ở nhóm đó.
4. Nếu muốn quay lại danh sách trước, bấm `Quay lại`.

Khi người dùng muốn bắt đầu khóa học mới:

1. Bấm `Chương trình học`.
2. Tìm khóa học muốn học.
3. Trên thẻ khóa học, bấm `Bắt đầu học`.
4. Khi cửa sổ `Bạn đã sẵn sàng bắt đầu khóa học?` hiện ra, đọc thông tin khóa học.
5. Bấm `Bắt đầu học ngay`.
6. Hệ thống đưa người dùng vào khóa học.

Khi người dùng muốn học tiếp khóa đã bắt đầu:

1. Bấm `Chương trình học`.
2. Tìm khóa học đang học.
3. Bấm `Tiếp tục học`.
4. Hệ thống mở phần học tiếp theo hoặc phần đang học dở.

Khi khóa học đã hoàn thành:

1. Trên thẻ khóa học có thể thấy trạng thái `Đã hoàn thành`.
2. Người dùng vẫn có thể mở lại khóa học nếu thẻ cho phép bấm vào.
3. Nếu có chứng chỉ hoặc kết quả, làm theo nút đang hiện trên thẻ khóa học.

## 6. Học bài trong khóa học

Khi người dùng đã vào khóa học:

1. Nếu có cửa sổ chào mừng khóa học, bấm `Tiếp tục học`.
2. Trên máy tính, nhìn sang cột bên trái có phần `Nội dung khoá học`.
3. Bấm vào tên bài học muốn mở.
4. Nếu có nhiều chương hoặc mục, bấm vào chương trước rồi chọn bài học bên trong.
5. Trên điện thoại, bấm nút ba gạch để mở danh sách bài học.
6. Trong bảng vừa mở, chọn tab `Nội dung` nếu cần.
7. Bấm vào tên bài học muốn học.

Khi người dùng muốn chuyển bài:

1. Mở bài học hiện tại.
2. Bấm `Tiếp tục` để đi sang phần tiếp theo.
3. Bấm `Quay lại` để về phần trước.
4. Nếu đến cuối bài hoặc cuối phần, bấm `Hoàn thành` nếu nút này xuất hiện.
5. Nếu thấy trạng thái `Đã hoàn thành`, phần đó đã được ghi nhận xong.

Khi người dùng muốn xem thông tin người hướng dẫn:

1. Vào khóa học.
2. Trên máy tính, nhìn cột thông tin bên phải hoặc bên trái tùy màn hình.
3. Tìm khu vực `Người hướng dẫn`, `Mentor`, `Giảng viên`, hoặc `Trợ giảng`.
4. Xem email, số điện thoại, và phần giới thiệu nếu có.
5. Trên điện thoại, bấm nút ba gạch rồi chọn tab `Thông tin và tài liệu`.

Khi người dùng muốn tải tài liệu tham khảo trong khóa:

1. Vào khóa học.
2. Tìm khu vực `Tài liệu tham khảo`.
3. Bấm vào tên tệp muốn tải.
4. Trình duyệt sẽ tải tệp về máy.
5. Nếu không thấy tài liệu, nghĩa là khóa học hiện chưa có tài liệu tham khảo được gắn.

## 7. Xem video trong bài học

Khi bài học có video:

1. Bấm nút phát ở giữa video hoặc bấm nút phát ở thanh điều khiển.
2. Nếu muốn tạm dừng, bấm lại nút phát hoặc nút tạm dừng.
3. Nếu muốn bật hoặc tắt tiếng, bấm nút âm thanh.
4. Nếu muốn xem lớn toàn màn hình, bấm nút toàn màn hình.
5. Xem gần hết video để hệ thống ghi nhận là đã học xong phần video.
6. Sau khi xem xong, bấm `Tiếp tục` hoặc `Hoàn thành` nếu nút đó xuất hiện.

Khi video chưa có nội dung:

1. Nếu thấy dòng `Video chưa được tải lên`, người dùng chưa thể xem video đó.
2. Chuyển sang phần khác nếu khóa học cho phép.
3. Báo lại cho người quản lý khóa học nếu cần.

## 8. Đọc nội dung, xem hình, xem sơ đồ

Khi bài học là nội dung đọc:

1. Mở bài học.
2. Đọc nội dung trên màn hình từ trên xuống dưới.
3. Nếu có hình ảnh, bấm vào hình để xem lớn hơn nếu giao diện cho phép.
4. Bấm `X` hoặc bấm ra ngoài vùng hình để đóng ảnh phóng to.
5. Khi đọc xong, bấm `Tiếp tục` hoặc `Hoàn thành`.

Khi bài học có sơ đồ hoặc hình minh họa:

1. Mở bài học.
2. Xem các khối thông tin trong sơ đồ.
3. Nếu có hình có thể phóng to, bấm vào hình để xem rõ hơn.
4. Sau khi xem xong, bấm `Tiếp tục` hoặc `Hoàn thành`.

Khi bài học có câu hỏi thường gặp:

1. Tìm phần `FAQ`.
2. Bấm vào câu hỏi muốn xem.
3. Câu trả lời sẽ mở ra bên dưới câu hỏi.
4. Bấm lại câu hỏi đó nếu muốn thu gọn.
5. Sau khi đọc xong, bấm `Tiếp tục` hoặc `Hoàn thành`.

## 9. Xem tài liệu PDF trong bài học

Khi bài học có tài liệu PDF:

1. Mở bài học có tài liệu.
2. Chờ tài liệu tải xong nếu thấy dòng `Đang tải tài liệu...`.
3. Cuộn trong khung tài liệu để đọc.
4. Nếu muốn mở tài liệu ở tab mới, bấm nút `Mở trong tab mới`.
5. Nếu muốn xem rộng hơn, bấm nút `Toàn màn hình`.
6. Nếu muốn thoát chế độ rộng, bấm `Thoát toàn màn hình`.
7. Khi đọc xong, bấm `Tiếp tục` hoặc `Hoàn thành`.

Khi không có tài liệu:

1. Nếu thấy `Chưa có tài liệu nào được cấu hình`, bài học hiện chưa có PDF.
2. Chuyển sang phần tiếp theo nếu khóa học cho phép.

## 10. Làm câu hỏi chọn đáp án

Khi bài học có câu hỏi một đáp án:

1. Đọc câu hỏi.
2. Đọc dòng nhắc `Chỉ chọn 1 đáp án.` nếu có.
3. Bấm vào một đáp án A, B, C, hoặc đáp án khác đang hiện.
4. Nếu cần hỗ trợ, bấm `Xem gợi ý`.
5. Nếu muốn ẩn gợi ý, bấm `Ẩn gợi ý`.
6. Sau khi chọn xong, bấm `Xác nhận`.
7. Nếu đúng, đọc phần `Đáp án & Giải thích` nếu có.
8. Bấm `Tiếp tục` hoặc `Hoàn thành` để sang phần sau.
9. Nếu chưa đúng, đọc thông báo rồi bấm `Thử lại`.

Khi bài học có câu hỏi nhiều đáp án:

1. Đọc câu hỏi.
2. Đọc dòng nhắc `Được phép chọn nhiều đáp án.` nếu có.
3. Bấm chọn tất cả đáp án mà người dùng cho là đúng.
4. Nếu bấm nhầm một đáp án, bấm lại đáp án đó để bỏ chọn.
5. Bấm `Xem gợi ý` nếu cần.
6. Bấm `Xác nhận`.
7. Nếu chưa đúng, bấm `Thử lại` rồi chọn lại.
8. Nếu đúng, bấm `Tiếp tục` hoặc `Hoàn thành`.

Khi bài học có câu hỏi dạng danh sách xổ xuống:

1. Đọc câu hỏi.
2. Bấm vào ô chọn đáp án.
3. Chọn một đáp án trong danh sách.
4. Bấm `Xác nhận`.
5. Nếu chưa đúng, bấm `Thử lại`.
6. Nếu đúng, bấm `Tiếp tục` hoặc `Hoàn thành`.

Khi bài học có câu hỏi nhập câu trả lời:

1. Đọc câu hỏi.
2. Bấm vào ô `Nhập câu trả lời...`.
3. Gõ câu trả lời.
4. Bấm `Xác nhận`.
5. Nếu chưa đúng, bấm `Thử lại`.
6. Nếu đúng, bấm `Tiếp tục` hoặc `Hoàn thành`.

## 11. Làm câu hỏi có ảnh hoặc video

Khi bài học có câu hỏi đi kèm ảnh hoặc video:

1. Xem ảnh hoặc video đang hiện.
2. Đọc câu hỏi.
3. Bấm chọn đáp án đúng.
4. Nếu hệ thống nhắc chọn nhiều đáp án, chọn tất cả đáp án đúng.
5. Bấm `Xem gợi ý` nếu cần.
6. Bấm `Xác nhận`.
7. Nếu đúng, bấm `Tiếp tục` để xem câu hoặc media tiếp theo.
8. Nếu chưa đúng, bấm `Thử lại`.
9. Khi hoàn tất, bấm `Đã hoàn thành` hoặc `Hoàn thành` nếu nút này xuất hiện.

Khi có nhiều câu trong cùng một phần:

1. Xem dòng `Câu hỏi X/Y` để biết đang ở câu số mấy.
2. Bấm `Quay lại` nếu muốn xem lại câu trước.
3. Bấm `Tiếp tục` sau khi trả lời đúng để sang câu sau.

## 12. Làm câu hỏi chọn ảnh

Khi bài học yêu cầu chọn một hình đúng:

1. Đọc câu hỏi.
2. Xem các ảnh đáp án A, B, C, hoặc các ảnh khác đang hiện.
3. Bấm vào ảnh mà người dùng chọn.
4. Nếu muốn xem ảnh rõ hơn, bấm nút phóng to trên ảnh.
5. Bấm `Đóng ảnh phóng to` hoặc `X` để đóng ảnh lớn.
6. Bấm `Xem gợi ý` nếu cần.
7. Bấm `Xác nhận`.
8. Nếu chưa đúng, bấm `Thử lại`.
9. Nếu đúng, đọc `Giải thích` nếu có rồi bấm `Tiếp tục` hoặc `Hoàn thành`.

## 13. Làm bài sắp xếp

Khi bài học yêu cầu sắp xếp thứ tự:

1. Đọc phần `Hướng dẫn`.
2. Bấm `Bắt đầu`.
3. Nhìn danh sách các dòng cần sắp xếp.
4. Kéo từng dòng lên hoặc xuống để đặt đúng thứ tự.
5. Kiểm tra lại thứ tự từ trên xuống dưới.
6. Bấm `Nộp bài chấm điểm`.
7. Nếu đúng, hệ thống ghi nhận hoàn thành.
8. Nếu chưa đúng, đọc thông báo rồi sắp xếp lại.
9. Sau khi hoàn thành, bấm `Tiếp tục` hoặc `Hoàn thành`.

## 14. Chơi ô chữ

Khi bài học có game ô chữ:

1. Đọc phần `Luật chơi cực đơn giản`.
2. Bấm `Bắt đầu`.
3. Bấm vào một dòng hoặc một ô trong bảng chữ.
4. Đọc câu hỏi gợi ý đang hiện.
5. Nhập chữ vào các ô trống.
6. Nếu cần gợi ý và có nút hình bóng đèn, bấm nút đó.
7. Có thể bấm vào câu hỏi trong phần `Danh sách câu hỏi` để nhảy tới dòng tương ứng.
8. Điền hết các ô trống.
9. Bấm `Nộp bài chấm điểm`.
10. Nếu hệ thống báo còn ô trống, quay lại điền đủ.
11. Nếu đúng, bấm `Tiếp tục` hoặc `Hoàn thành`.

## 15. Làm bài chat tình huống

Khi bài học là cuộc trò chuyện tình huống:

1. Đọc phần `Hướng dẫn`.
2. Bấm `Bắt đầu`.
3. Đọc đoạn hội thoại đang hiện.
4. Chọn một đáp án A, B, C, hoặc lựa chọn phù hợp.
5. Nếu chọn chưa đúng, bấm `Thử lại`.
6. Nếu chọn đúng và còn câu tiếp theo, bấm `Tiếp tục`.
7. Nếu có nút `Xem các câu khác`, bấm để xem thêm các tình huống khác.
8. Nếu có nút `Xem đáp án đúng`, bấm để xem đáp án mẫu.
9. Khi thấy `Đã hoàn thành`, phần này đã xong.
10. Bấm `Tiếp tục` hoặc `Hoàn thành` để sang phần sau.

## 16. Nộp bài tập trong khóa học

Khi người dùng muốn mở bài tập:

1. Vào khóa học.
2. Ở danh sách bên trái, tìm phần `Bài tập`.
3. Bấm vào tên bài tập muốn làm.
4. Nếu đang dùng điện thoại, bấm nút ba gạch trước rồi tìm phần `Bài tập`.

Khi bài tập chưa mở được:

1. Nếu thấy `Học xong nội dung khóa học để mở nộp bài`, người dùng cần học xong các nội dung yêu cầu trước.
2. Quay lại danh sách bài học.
3. Hoàn thành các bài học còn thiếu.
4. Mở lại bài tập sau khi học xong.

Khi bài tập đã hết hạn:

1. Nếu thấy `Đã hết thời hạn nộp bài`, người dùng không thể nộp theo cách bình thường.
2. Đọc thông tin hạn nộp trên màn hình.
3. Liên hệ người quản lý khóa học nếu cần hỗ trợ.

Khi người dùng làm bài tập:

1. Mở bài tập.
2. Đọc phần `Câu hỏi`.
3. Nếu có `Tệp đính kèm từ quản trị viên`, bấm vào tên tệp để tải về đọc.
4. Tìm phần `Bài làm`.
5. Bấm vào ô nhập câu trả lời.
6. Nhập nội dung bài làm.
7. Nếu cần gửi tệp kèm theo, bấm `Chọn tệp`.
8. Chọn tệp từ máy.
9. Nếu chọn nhầm tệp, bấm nút `Xóa tệp` hoặc nút `X` cạnh tệp đó.
10. Kiểm tra lại câu trả lời và tệp đính kèm.
11. Bấm `Nộp bài`.

Khi người dùng muốn nộp lại:

1. Mở bài tập đã nộp.
2. Nếu có nút `Nộp lại`, chỉnh sửa câu trả lời hoặc tệp đính kèm.
3. Bấm `Nộp lại`.
4. Nếu không thấy nút này, bài tập có thể không cho nộp lại.

Khi người dùng muốn xem phản hồi bài tập:

1. Mở bài tập.
2. Tìm phần `Phản hồi`.
3. Xem `Điểm` nếu có.
4. Đọc `Lời nhận xét`.
5. Nếu có tệp phản hồi, bấm vào tên tệp để tải về.

## 17. Dùng Thư viện

Khi người dùng muốn tìm tài liệu:

1. Bấm `Thư viện`.
2. Bấm vào ô `Tìm kiếm tài liệu...`.
3. Nhập tên tài liệu hoặc từ khóa cần tìm.
4. Xem danh sách tài liệu hiện ra.

Khi người dùng muốn xem tài liệu theo danh mục:

1. Bấm `Thư viện`.
2. Tìm phần `Danh mục tài liệu`.
3. Bấm vào danh mục muốn xem.
4. Danh sách sẽ chuyển sang tài liệu thuộc danh mục đó.
5. Nếu muốn xem lại tất cả, bấm `Xem tất cả` nếu nút này xuất hiện.
6. Nếu đang ở trang danh mục chi tiết, bấm `Quay lại` để trở về.

Khi dùng điện thoại và muốn xem toàn bộ danh mục:

1. Bấm `Thư viện`.
2. Ở phần `Danh mục tài liệu`, bấm `Xem tất cả`.
3. Màn hình `Tất cả danh mục` hiện ra.
4. Bấm vào ô `Tìm kiếm danh mục...` nếu muốn tìm danh mục.
5. Chọn danh mục cần xem.
6. Bấm mũi tên quay lại nếu muốn đóng danh sách danh mục.

Khi người dùng muốn lọc loại tài liệu:

1. Mở `Thư viện`.
2. Nếu đang trong trang danh mục, bấm `Lọc`.
3. Chọn `Tất cả định dạng`, `PDF`, `DOCX`, `XLSX`, `PPTX`, `MP4`, hoặc `Hình ảnh`.
4. Danh sách tài liệu sẽ đổi theo định dạng đã chọn.

Khi người dùng muốn xem trước tài liệu:

1. Mở `Thư viện`.
2. Tìm tài liệu cần xem.
3. Bấm vào dòng tài liệu hoặc thẻ tài liệu.
4. Cửa sổ xem trước mở ra.
5. Đọc hoặc xem nội dung trong cửa sổ.
6. Nếu muốn tải về, bấm `Tải về máy`.
7. Bấm `X` để đóng cửa sổ xem trước.

Khi tài liệu không xem trước được:

1. Nếu thấy `Chưa hỗ trợ xem trước`, bấm `Tải về ngay` để tải tệp về máy.
2. Nếu thấy `Tệp quá lớn`, bấm `Tải file trực tiếp`.
3. Mở tệp bằng ứng dụng phù hợp trên máy của người dùng.

Khi người dùng muốn tải tài liệu ngay từ danh sách:

1. Mở `Thư viện`.
2. Tìm tài liệu cần tải.
3. Bấm nút hình tải xuống ở dòng tài liệu.
4. Trình duyệt tải tài liệu về máy.

Khi danh sách có nhiều trang:

1. Nhìn phần cuối danh sách.
2. Xem dòng `Trang X / Y`.
3. Bấm nút mũi tên sang phải để qua trang sau.
4. Bấm nút mũi tên sang trái để về trang trước.
5. Nếu muốn đổi số tài liệu mỗi trang, bấm ô `Hiển thị`.
6. Chọn `5 / trang`, `10 / trang`, `20 / trang`, hoặc `50 / trang`.

## 18. Xem Danh hiệu

Khi người dùng muốn xem danh hiệu:

1. Bấm `Danh hiệu` trên thanh menu.
2. Xem khu vực `Danh Hiệu Của Bạn`.
3. Xem `Tiến độ tổng thể` để biết đã đạt bao nhiêu danh hiệu.
4. Xem dòng dạng `x/y danh hiệu` và phần trăm hoàn thành.

Khi người dùng muốn lọc danh hiệu:

1. Vào trang `Danh hiệu`.
2. Trên máy tính, bấm `Tất cả`, `Đã đạt`, `Chưa đạt`, hoặc một nhóm danh hiệu.
3. Trên điện thoại, bấm nút lọc đang hiển thị.
4. Chọn `Tất cả`, `Đã đạt`, `Chưa đạt`, hoặc nhóm danh hiệu muốn xem.
5. Danh sách danh hiệu sẽ đổi theo lựa chọn.

Khi người dùng muốn xem danh hiệu đã đạt:

1. Vào trang `Danh hiệu`.
2. Bấm `Đã đạt`.
3. Bấm vào thẻ danh hiệu muốn xem.
4. Hình danh hiệu mở lớn trên màn hình.
5. Bấm `X` để đóng.

Khi người dùng muốn xem điều kiện của danh hiệu chưa đạt:

1. Vào trang `Danh hiệu`.
2. Bấm `Chưa đạt`.
3. Bấm vào thẻ danh hiệu đang có biểu tượng khóa.
4. Mặt sau thẻ sẽ hiện mô tả hoặc tiến độ.
5. Bấm lại thẻ hoặc bấm theo dòng `Chạm để quay lại` để quay về mặt trước.

Khi không có danh hiệu phù hợp:

1. Nếu thấy `Không có danh hiệu nào phù hợp với bộ lọc`, hãy chọn bộ lọc khác.
2. Có thể quay lại `Tất cả` để xem toàn bộ.

## 19. Xem và sửa hồ sơ cá nhân

Khi người dùng muốn mở hồ sơ:

1. Bấm ảnh đại diện ở góc trên bên phải.
2. Bấm `Hồ sơ cá nhân`.
3. Màn hình `Hồ Sơ Cá Nhân` mở ra.

Khi người dùng muốn đổi ảnh đại diện:

1. Vào `Hồ sơ cá nhân`.
2. Bấm `Đổi ảnh đại diện`.
3. Chọn ảnh từ máy.
4. Chờ hệ thống tải ảnh lên.
5. Nếu ảnh quá lớn hoặc sai định dạng, chọn ảnh khác.

Khi người dùng muốn xem ảnh đại diện lớn hơn:

1. Vào `Hồ sơ cá nhân`.
2. Bấm vào ảnh đại diện.
3. Xem ảnh lớn.
4. Bấm `X` hoặc bấm ra ngoài để đóng.

Khi người dùng muốn sửa thông tin cá nhân:

1. Vào `Hồ sơ cá nhân`.
2. Sửa ô `Tên hiển thị`.
3. Chọn `Giới tính` nếu muốn.
4. Chọn `Quốc gia` nếu muốn.
5. Chọn `Trình độ học vấn` nếu muốn.
6. Chọn `Ngôn ngữ` nếu muốn.
7. Nhập `Năm sinh` nếu muốn.
8. Nhập `Số điện thoại` nếu muốn.
9. Nhập `Tiểu sử (Bio)` nếu muốn.
10. Bấm `LƯU THAY ĐỔI`.

Lưu ý khi sửa hồ sơ:

- `Tên đăng nhập (Username)` thường không sửa được.
- `Địa chỉ Email` thường không sửa được.
- Nếu nút lưu không bấm được trong chế độ xem thử, người dùng cần dùng tài khoản thật hoặc thoát khỏi chế độ xem thử.

## 20. Đổi mật khẩu

Khi người dùng muốn đổi mật khẩu:

1. Bấm ảnh đại diện ở góc trên bên phải.
2. Bấm `Đổi mật khẩu`.
3. Nhập mật khẩu đang dùng ở ô `Mật khẩu hiện tại`.
4. Nhập mật khẩu mới ở ô `Mật khẩu mới`.
5. Nhập lại mật khẩu mới ở ô `Nhập lại mật khẩu mới`.
6. Nếu muốn xem mật khẩu vừa nhập, bấm nút hình con mắt ở cạnh ô.
7. Bấm `Xác nhận đổi mật khẩu`.

Khi người dùng không muốn đổi nữa:

1. Trong cửa sổ đổi mật khẩu, bấm `Hủy`.
2. Hoặc bấm `X` nếu có nút đóng.

Khi đổi mật khẩu không thành công:

1. Kiểm tra lại mật khẩu hiện tại.
2. Kiểm tra mật khẩu mới có đủ yêu cầu tối thiểu không.
3. Kiểm tra hai ô mật khẩu mới có nhập giống nhau không.
4. Bấm `Xác nhận đổi mật khẩu` lần nữa sau khi sửa.

## 21. Xem thông báo

Khi người dùng muốn xem thông báo nhanh:

1. Bấm nút hình chuông ở góc trên bên phải.
2. Danh sách `Thông báo` mở ra.
3. Bấm vào một thông báo để xem hoặc mở nội dung liên quan.
4. Nếu muốn xem đầy đủ, bấm `Xem tất cả thông báo`.

Khi người dùng muốn xem toàn bộ thông báo:

1. Bấm nút hình chuông.
2. Bấm `Xem tất cả thông báo`.
3. Cửa sổ `Tất cả thông báo` mở ra.
4. Cuộn để xem các thông báo.
5. Nếu muốn đánh dấu đã đọc, bấm `Đánh dấu đã đọc`.
6. Bấm `X` để đóng cửa sổ.

Khi thông báo là phản hồi bài tập:

1. Bấm nút hình chuông.
2. Bấm vào thông báo phản hồi bài tập.
3. Cửa sổ `Phản hồi bài tập` mở ra.
4. Xem tên khóa học, tên bài tập, câu hỏi, lời nhận xét, điểm số và tệp phản hồi nếu có.
5. Nếu có tệp phản hồi, bấm vào tên tệp để tải.
6. Bấm `Đã hiểu` để đóng.

Khi không có thông báo:

1. Nếu thấy `Chưa có thông báo mới`, hiện chưa có thông báo mới.
2. Nếu thấy `Chưa có thông báo nào`, tài khoản chưa có thông báo nào được gửi.

## 22. Dùng trợ lý AI trong giao diện

Khi người dùng muốn mở trợ lý AI:

1. Bấm nút tròn nổi có biểu tượng chat, thường nằm ở góc dưới bên phải.
2. Nếu thấy `Đang kết nối...`, chờ trong giây lát.
3. Nếu thấy danh sách trợ lý, bấm chọn trợ lý muốn trò chuyện.
4. Nếu thấy danh sách hội thoại, bấm một hội thoại cũ hoặc bấm `Mới` để tạo hội thoại mới.

Khi người dùng muốn gửi tin nhắn:

1. Mở cửa sổ chat.
2. Bấm vào ô `Nhập tin nhắn...`.
3. Gõ câu hỏi hoặc nội dung muốn hỏi.
4. Bấm nút gửi ở cạnh ô nhập.
5. Chờ trợ lý trả lời.

Khi người dùng muốn xem tin nhắn cũ:

1. Mở hội thoại.
2. Bấm `Xem tin nhắn cũ hơn` nếu nút này xuất hiện.
3. Cuộn lên để đọc lại.

Khi người dùng muốn tạo hội thoại mới:

1. Mở cửa sổ chat.
2. Ở màn hình `Hội thoại`, bấm `Mới`.
3. Chọn trợ lý nếu hệ thống yêu cầu.
4. Nhập tin nhắn đầu tiên.
5. Bấm nút gửi.

Khi người dùng muốn xóa hội thoại:

1. Mở cửa sổ chat.
2. Vào danh sách `Hội thoại`.
3. Bấm nút hình thùng rác cạnh hội thoại muốn xóa.
4. Khi cửa sổ `Xoá hội thoại` hiện ra, bấm `Xoá` để xác nhận.
5. Nếu đổi ý, bấm `Huỷ`.

Khi người dùng muốn phóng to cửa sổ chat:

1. Mở cửa sổ chat.
2. Bấm nút `Phóng to`.
3. Nếu muốn thu nhỏ lại, bấm `Thu nhỏ`.

Khi người dùng muốn nói bằng micro:

1. Mở cửa sổ chat.
2. Bấm nút micro hoặc nút `Nói bằng micro`.
3. Nếu trình duyệt hỏi quyền dùng micro, chọn cho phép.
4. Nói nội dung muốn gửi.
5. Bấm `Dừng nghe` hoặc nút tương ứng nếu muốn dừng.
6. Nếu không dùng được micro, nhập tin nhắn bằng bàn phím.

Khi không có trợ lý AI:

1. Nếu thấy `Chưa có trợ lý AI nào được kích hoạt`, tài khoản hiện chưa có trợ lý để dùng.
2. Người dùng cần liên hệ quản trị viên hoặc người quản lý khóa học.

## 23. Đổi sáng tối và màu giao diện

Khi người dùng muốn đổi sáng tối:

1. Nhìn góc trên bên phải.
2. Bấm nút hình mặt trăng hoặc mặt trời.
3. Giao diện sẽ chuyển giữa chế độ sáng và chế độ tối.

Khi người dùng muốn đổi màu giao diện:

1. Nhìn góc trên bên phải.
2. Bấm nút hình bảng màu nếu nút này xuất hiện.
3. Chọn màu hoặc kiểu màu mong muốn.
4. Giao diện đổi màu theo lựa chọn.

## 24. Chuyển tổ chức, vào quản trị, đăng xuất

Các mục trong phần này chỉ hiện với một số tài khoản nhất định.

Khi người dùng muốn mở menu tài khoản:

1. Bấm ảnh đại diện ở góc trên bên phải.
2. Danh sách lựa chọn tài khoản sẽ mở ra.

Khi người dùng muốn vào trang quản trị:

1. Bấm ảnh đại diện.
2. Bấm `Quản trị hệ thống` nếu mục này xuất hiện.
3. Hệ thống mở khu vực quản trị.

Khi người dùng muốn chuyển tổ chức:

1. Bấm ảnh đại diện.
2. Bấm `Chuyển tổ chức` nếu mục này xuất hiện.
3. Chọn tổ chức muốn chuyển sang.
4. Chờ hệ thống đổi sang tổ chức đó.

Khi người dùng muốn đăng xuất:

1. Bấm ảnh đại diện ở góc trên bên phải.
2. Bấm `Đăng xuất`.
3. Hệ thống đưa người dùng ra khỏi tài khoản.

## 25. Tìm kiếm trên điện thoại

Khi người dùng muốn tìm nhanh trên điện thoại:

1. Vào một trong các trang có tìm kiếm như `Khám phá`, `Chương trình học`, hoặc `Thư viện`.
2. Bấm nút hình kính lúp ở thanh trên.
3. Nhập từ khóa vào ô `Tìm kiếm...`.
4. Xem kết quả hiện ra.
5. Bấm `X` hoặc quay lại nếu muốn đóng tìm kiếm.

## 26. Các tình huống thường gặp

Khi người dùng không thấy khóa học:

1. Bấm `Chương trình học`.
2. Chọn `Tất cả`.
3. Xóa chữ trong ô tìm kiếm nếu đang có.
4. Chọn `Tất cả danh mục` nếu đang lọc danh mục.
5. Nếu vẫn không thấy, tài khoản có thể chưa được cấp khóa học.

Khi người dùng không thấy thư viện:

1. Kiểm tra xem có mục `Thư viện` trên thanh menu không.
2. Nếu không có, tài khoản hoặc đơn vị có thể chưa bật thư viện.
3. Liên hệ người quản lý nếu cần dùng tài liệu.

Khi người dùng không thấy danh hiệu:

1. Kiểm tra xem có mục `Danh hiệu` trên thanh menu không.
2. Nếu không có, đơn vị có thể chưa bật tính năng danh hiệu.
3. Nếu có mục này nhưng không thấy danh hiệu, thử chọn bộ lọc `Tất cả`.

Khi người dùng không tải được tài liệu:

1. Thử bấm lại nút tải xuống.
2. Nếu đang trong cửa sổ xem trước, bấm `Tải về máy`, `Tải về ngay`, hoặc `Tải file trực tiếp`.
3. Kiểm tra trình duyệt có chặn tải xuống không.
4. Nếu vẫn không tải được, báo người quản lý.

Khi người dùng không nộp được bài:

1. Kiểm tra bài tập có bị khóa không.
2. Kiểm tra bài tập đã hết hạn chưa.
3. Kiểm tra ô `Bài làm` đã có nội dung chưa.
4. Nếu có yêu cầu tệp đính kèm, kiểm tra đã chọn đúng tệp chưa.
5. Bấm `Nộp bài` hoặc `Nộp lại` sau khi kiểm tra.

Khi người dùng làm quiz không qua:

1. Đọc lại câu hỏi.
2. Bấm `Xem gợi ý` nếu có.
3. Chọn lại đáp án.
4. Bấm `Xác nhận`.
5. Nếu vẫn sai, bấm `Thử lại` và đọc phần giải thích nếu hệ thống có hiển thị.

Khi người dùng bị kẹt trong một cửa sổ:

1. Tìm nút `X`.
2. Nếu có nút `Hủy`, bấm `Hủy`.
3. Nếu có mũi tên quay lại, bấm mũi tên quay lại.
4. Nếu vẫn không thoát được, tải lại trang.

## 27. Câu trả lời mẫu cho AI

Khi người dùng hỏi "làm sao để học tiếp":

1. Hướng dẫn: bấm `Khám phá`.
2. Tìm phần `Tiếp tục học`.
3. Bấm `Tiếp tục học` trên khóa học muốn học.

Khi người dùng hỏi "làm sao để bắt đầu khóa mới":

1. Hướng dẫn: bấm `Chương trình học`.
2. Tìm khóa học.
3. Bấm `Bắt đầu học`.
4. Bấm tiếp `Bắt đầu học ngay` trong cửa sổ xác nhận.

Khi người dùng hỏi "làm sao để nộp bài":

1. Hướng dẫn: vào khóa học.
2. Bấm bài tập trong phần `Bài tập`.
3. Nhập nội dung ở phần `Bài làm`.
4. Bấm `Chọn tệp` nếu cần đính kèm.
5. Bấm `Nộp bài`.

Khi người dùng hỏi "làm sao để xem tài liệu":

1. Hướng dẫn: bấm `Thư viện`.
2. Tìm tài liệu bằng ô `Tìm kiếm tài liệu...` hoặc chọn `Danh mục tài liệu`.
3. Bấm vào tài liệu để xem trước.
4. Bấm `Tải về máy` nếu muốn tải.

Khi người dùng hỏi "làm sao để đổi mật khẩu":

1. Hướng dẫn: bấm ảnh đại diện.
2. Bấm `Đổi mật khẩu`.
3. Nhập mật khẩu hiện tại.
4. Nhập mật khẩu mới hai lần.
5. Bấm `Xác nhận đổi mật khẩu`.

Khi người dùng hỏi "làm sao để xem phản hồi bài tập":

1. Hướng dẫn: bấm nút hình chuông.
2. Bấm thông báo phản hồi bài tập nếu có.
3. Hoặc vào lại khóa học, mở bài tập đã nộp.
4. Xem phần `Phản hồi`.

Khi người dùng hỏi "làm sao để dùng AI chat":

1. Hướng dẫn: bấm nút chat tròn ở góc dưới bên phải.
2. Chọn trợ lý hoặc bấm `Mới`.
3. Nhập câu hỏi vào ô `Nhập tin nhắn...`.
4. Bấm nút gửi.

Khi người dùng hỏi "làm sao để xem danh hiệu":

1. Hướng dẫn: bấm `Danh hiệu`.
2. Xem `Tiến độ tổng thể`.
3. Bấm `Đã đạt` để xem danh hiệu đã nhận.
4. Bấm `Chưa đạt` để xem danh hiệu chưa nhận và điều kiện cần làm.
